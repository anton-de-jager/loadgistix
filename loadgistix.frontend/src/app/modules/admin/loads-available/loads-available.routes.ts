import { Routes } from '@angular/router';
import { LoadsAvailableComponent } from 'app/modules/admin/loads-available/loads-available.component';

export default [
    {
        path     : '',
        component: LoadsAvailableComponent,
    },
] as Routes;
