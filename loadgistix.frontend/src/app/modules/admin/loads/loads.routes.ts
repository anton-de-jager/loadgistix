import { Routes } from '@angular/router';
import { LoadsComponent } from 'app/modules/admin/loads/loads.component';

export default [
    {
        path     : '',
        component: LoadsComponent,
    },
] as Routes;
